<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="description" content="An AI-based dashboard to monitor and predict Air Quality Index (AQI) in real-time, providing health advisories and policy insights.">
  <title>Vaayu | <%= typeof title !== 'undefined' ? title : 'Dashboard' %></title>


  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>

  <!-- Flatpickr -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">

 <style>
  :root {
    --nav-bg: linear-gradient(90deg, rgba(173, 216, 230, 0.8), rgba(224, 247, 250, 0.75));
    --nav-border: rgba(255, 255, 255, 0.3);
    --nav-shadow: rgba(0, 0, 0, 0.1);
    --accent-gradient: linear-gradient(135deg, #00b4db, #0083b0);
    --text-dark: #1b2a49;
  }

  /* 🌬️ Glass Navbar */
.navbar-glass {
  backdrop-filter: blur(18px);             /* glass blur */
  background: var(--nav-bg);               /* white glass bg */
  border-bottom: 1px solid var(--nav-border);
  box-shadow: 0 6px 20px var(--nav-shadow);
  padding: 0.8rem 2rem;
  position: fixed;
  width: 100%;
  z-index: 1000;
  top: 0;                                  /* stick to top */
  left: 0;
}

  .navbar-brand {
    font-weight: 700;
    font-size: 1.5rem;
    background: var(--accent-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .nav-link {
    color: var(--text-dark) !important;
    font-weight: 500;
    transition: all 0.3s ease;
    border-radius: 6px;
    padding: 6px 10px;
  }

  .nav-link:hover,
  .nav-link.active {
    background: rgba(255, 255, 255, 0.4);
    color: #0077b6 !important;
  }

  /* 🌫️ Dropdown Selects */
  .form-select {
    background: rgba(255, 255, 255, 0.6);
    border: 1px solid var(--nav-border);
    font-size: 0.85rem;
    color: var(--text-dark);
    border-radius: 6px;
  }

  /* ☀️/🌙 Theme Button */
  .btn-theme {
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.6);
    border: 1px solid var(--nav-border);
    color: var(--text-dark);
    transition: all 0.3s ease;
  }
  .btn-theme:hover {
    background: var(--accent-gradient);
    color: #fff;
    transform: translateY(-1px);
  }

  /* ✨ Login Button */
  .btn-login {
    background: var(--accent-gradient);
    color: #fff;
    font-weight: 600;
    border-radius: 6px;
    border: none;
    padding: 6px 14px;
    transition: all 0.3s ease;
  }
  .btn-login:hover {
    opacity: 0.9;
    transform: translateY(-1px);
  }
</style>

<nav class="navbar navbar-expand-lg navbar-glass">
  <div class="container-fluid px-4">
    <a class="navbar-brand" href="#"><i class="fas fa-wind"></i> AQI Dashboard</a>
    <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navMenu">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navMenu">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link active" href="/dashboard">Dashboard</a></li>
        <li class="nav-item"><a class="nav-link" href="/health">Health</a></li>
        <li class="nav-item"><a class="nav-link" href="/citizen">citizen</a></li>
        <li class="nav-item"><a class="nav-link" href="/policyMap">Policy</a></li>
      </ul>
      <div class="d-flex align-items-center gap-2">
        <select class="form-select form-select-sm">
          <option selected>AQI-US</option>
          <option>AQI-IN</option>
        </select>
        <select class="form-select form-select-sm">
          <option selected>English-IN</option>
          <option>हिंदी</option>
        </select>
        <button class="btn btn-theme btn-sm"><i class="fas fa-moon"></i></button>
        <button class="btn btn-login btn-sm">Login</button>
      </div>
    </div>
  </div>
</nav></script>
</head>

<body>
  

  <!-- Main Content -->
  <div class="container content">
    <%- body %>
  </div>

<footer class="footer mt-auto py-3 ">
  <div class="container text-center">
    <span class="text-muted">© 2025 Vaayu. All rights reserved.</span>
 <p class="text-muted small mb-0">
      Disclaimer: The AQI data presented is aggregated from various sources and is intended for informational and educational use only. It should not be considered a substitute for professional medical advice regarding health conditions.
    </p>
  </div>
</footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Flatpickr -->
  <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>

  <script>
    window.addEventListener("load", () => {
      const loader = document.getElementById("loader");
      loader.classList.add("fade-out");
      setTimeout(() => loader.style.display = "none", 500);
    });
  </script>
</body>
</html>



